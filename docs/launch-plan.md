# Casa Playa — πλάνο μέχρι το live

Κατάσταση: **2026-07-30**. Ενημερώνεται καθώς κλείνουν τα σημεία.

Κρίσιμη διαδρομή: η αίτηση test account στη Euronet θέλει **ΑΦΜ + διακριτικό τίτλο**
(από τον πελάτη) και **ενεργό subdomain** (από τη ZanteWize). Όλα τα υπόλοιπα
τρέχουν παράλληλα.

---

## ✅ Έτοιμα

- Ticketing / HashKey / callback / idempotency — `src/lib/paycenter/`, επαληθευμένα με το
  παράδειγμα του Παραρτήματος 4 του εγχειριδίου.
- Το **WSDL διαβάστηκε από τον ίδιο τον server** (2026-07-30): operation `IssueNewTicket`,
  namespace `http://piraeusbank.gr/paycenter/redirection`, τα πεδία σε `<Request>` τύπου
  `TicketRequest`. Ταιριάζει με τις προεπιλογές στο `config.ts` — **δεν χρειάζεται
  ερώτημα στη Euronet**.
- Κλείδωμα προ-λανσαρίσματος (HTTP Basic Auth + noindex) — `src/lib/site-gate.ts`,
  `src/proxy.ts`. Ανοιχτά μόνο τα `/api/payment/success` και `/api/payment/failure`.
- **Στατική IP: `46.224.113.245`** — Hetzner CPX12, Nuremberg, `casa-playa-proxy`.
  Επαληθεύτηκε ότι η εξερχόμενη κίνηση φεύγει από αυτήν και ότι το paycenter απαντά
  `200` από αυτό το μηχάνημα. Primary IP με auto-delete **off** + protection **on**.

---

## A. ZanteWize (κατασκευαστής/host του casaplaya.gr)

Ένα email, από τον υπεύθυνο της επιχείρησης. Κείμενο: `docs/email-zantewize.md`.

- [x] **CNAME `booking`** (2026-07-31) → `955f81286e545e4d.vercel-dns-017.com`, DNS only.
      Επαληθεύτηκε ότι λύνεται σε IP του Vercel και όχι του Cloudflare.
- [ ] **Διαπιστευτήρια Mailjet** (SMTP ή API) για αποστολή ως `kratiseis@casaplaya.gr`.
      Το SPF του domain ήδη εξουσιοδοτεί `spf.mailjet.com`, οπότε δεν θέλει αλλαγή SPF.
- [x] **DKIM** (2026-07-31) — δημοσιεύτηκε στον selector `mailjet._domainkey`.
- [x] **DMARC** (2026-07-31) — `v=DMARC1; p=none; rua=mailto:bananacasaplaya@gmail.com`.
      Το `rua` είναι αδρανές (το `gmail.com` δεν μπορεί να δημοσιεύσει την εγγραφή
      έγκρισης για αναφορές άλλου domain) αλλά δεν βλάπτει· η πολιτική ισχύει κανονικά.
- [ ] **Εικονίδια/σήματα καρτών στην αρχική** του `casaplaya.gr` (Visa, Mastercard,
      Maestro, Visa Secure, Mastercard Identity Check) — απαίτηση §8 του εγχειριδίου.
- [ ] **Αργότερα, μόνο μετά από ρητή εντολή:** το BOOK NOW από
      `casaplaya.travelotopos.com/?cids=1` στο `booking.casaplaya.gr`.

## B. Υπεύθυνος Casa Playa

- [x] **Στοιχεία επιχείρησης — επιβεβαιωμένα από το μητρώο** (2026-07-31), μέσω
      αναζήτησης VIES στο `EL801773715`, που αντλεί από το μητρώο ΑΑΔΕ:

      | | |
      |---|---|
      | ΑΦΜ | `801773715` (έγκυρο ψηφίο ελέγχου· αρχίζει από 8 = νομικό πρόσωπο) |
      | Επωνυμία | `CASA PLAYA ΟΕ` |
      | Διακριτικός τίτλος | `BANANA BEACH CASA PLAYA` |
      | Έδρα | `ΒΑΣΙΛΙΚΟΣ, 29100 ΖΑΚΥΝΘΟΣ` — πλήρης· ο Βασιλικός είναι χωριό, δεν υπάρχει οδός/αριθμός |

      Στη σύμβαση γράφονται **ακριβώς** έτσι. Στο VIES η επωνυμία εμφανίζεται χωρίς
      τελείες («ΟΕ», όχι «Ο.Ε.»).

      > **Συνέπεια για το site:** ο διακριτικός τίτλος είναι αυτό που τυπώνεται στο
      > extrait της κάρτας, δηλαδή ο πελάτης θα δει χρέωση **BANANA BEACH CASA PLAYA**
      > ενώ κράτησε σε site με το όνομα «Casa Playa». Πρέπει να εμφανίζεται και στη
      > σελίδα πληρωμής και στο footer, αλλιώς ακολουθούν αμφισβητήσεις χρέωσης.

      (Από το footer του παλιού site επίσης: τηλ. 26950 35160 / 697 69 57 613,
      bananacasaplaya@gmail.com.)
- [ ] Κείμενο **πολιτικής ακύρωσης** και όρων, ακριβώς όπως θα εμφανίζεται.
- [ ] Κείμενο **πολιτικής απορρήτου** (GDPR).
- [ ] **Χωρητικότητα ανά σειρά/προϊόν** — το `dailyCapacity` είναι ακόμα `null`, άρα δεν
      επιβάλλεται όριο.
- [ ] **Εστιατόριο**: άτομα ανά slot, και αν το τραπέζι προπληρώνεται.
- [ ] **Εκπτωτικοί κωδικοί** στο λανσάρισμα ναι/όχι (αν όχι, κρύβουμε το πεδίο).
- [ ] Ποιοι θα έχουν πρόσβαση στο **AdminTool** της Euronet.
- [ ] Διεύθυνση email της επιχείρησης για τις **αυτόματες ειδοποιήσεις** συναλλαγών.

## Γ. Υποδομή (εμείς)

Στήθηκε 31/07/2026. Τεκμηρίωση, config και script ελέγχου: `infra/proxy/`.

- [x] Proxy στον Hetzner: **Squid 7.2, θύρα 8443**, TLS με **αυτο-υπογεγραμμένο
      πιστοποιητικό 10ετίας, pinned** στην εφαρμογή (SAN `IP:46.224.113.245`· χωρίς
      εξάρτηση από DNS/Let's Encrypt, χωρίς ανανεώσεις).
- [x] Αυθεντικοποίηση στον proxy με μυστικό (χρήστης `casaplaya`) — **όχι** περιορισμό
      IP, γιατί το Vercel δεν έχει σταθερή IP εξόδου.
- [x] Περιορισμός upstream **αποκλειστικά** στο `paycenter.piraeusbank.gr:443`·
      οτιδήποτε άλλο παίρνει `403` πριν καν ζητηθεί συνθηματικό.
- [x] Firewall με **`ufw` στο ίδιο το μηχάνημα** (αντί για Hetzner Cloud Firewall, που
      θέλει API token): δεκτά μόνο 22 και 8443, όλα τα άλλα εισερχόμενα απορρίπτονται.
      SSH **μόνο με κλειδί** — το password auth απενεργοποιήθηκε.
- [x] SSH **δεν** περιορίστηκε σε μία IP πηγής (απόφαση 31/07/2026): η δική μας IP δεν
      είναι εγγυημένα σταθερή και το κέρδος είναι μικρό όταν το password auth είναι
      ήδη κλειστό. Αν χρειαστεί:
      `ufw delete allow 22/tcp && ufw allow from <IP> to any port 22 proto tcp`.
- [x] Αυτόματες ενημερώσεις ασφαλείας — `unattended-upgrades`, auto-reboot 04:30.
- [ ] **Snapshot** μετά το στήσιμο — από το Hetzner console.
- [x] Στην εφαρμογή: κλήση του ticketing μέσω `undici` ProxyAgent με το pinned CA —
      `src/lib/paycenter/egress.ts`. Το σκέτο `HTTPS_PROXY` **δεν** το τιμά το `fetch`
      του Node· ούτε δέχεται dispatcher άλλης έκδοσης undici, γι' αυτό
      χρησιμοποιείται το `fetch` της ίδιας της undici. Ελεγμένο άκρη-σε-άκρη με το
      πραγματικό WSDL (`node infra/proxy/check.mjs`).
- [x] Vercel: `PAYCENTER_PROXY_URL`, `_USER`, `_PASSWORD`, `_CA` (2026-07-31, Production
      + Preview). Αδρανείς όσο το `PAYCENTER_MODE` είναι `mock`.
- [x] Vercel: προσθήκη `booking.casaplaya.gr` (2026-07-30). CNAME target που δόθηκε:
      `955f81286e545e4d.vercel-dns-017.com`.
- [x] Vercel: `APP_BASE_URL`, `SITE_GATE_USER`, `SITE_GATE_PASSWORD` (2026-07-31).
      Το `booking.casaplaya.gr` σερβίρει με πιστοποιητικό Let's Encrypt και είναι
      κλειδωμένο· ανοιχτά μόνο τα δύο URL απάντησης της τράπεζας.
- [x] **Vercel Pro** (2026-07-31). Το Hobby επιτρέπεται μόνο για μη εμπορική χρήση, οπότε
      ήταν προϋπόθεση για παραγωγική λειτουργία. Λειτουργικά κόστη: Vercel Pro + Hetzner
      CPX12 (~€14.25/μήνα) + Primary IP (€0.62/μήνα).

## Δ. Εφαρμογή

- [x] Ημερολόγιο διαθεσιμότητας ανά προϊόν, επιλογή slot/ποσότητας, ζωντανή τιμή με ΦΠΑ
      — `src/components/booking-form.tsx` + `src/lib/availability.ts` (τα τραπέζια
      κρατιούνται 2.5 ώρες, οπότε το slot ελέγχεται σε όλο το παράθυρο κατοχής).
- [x] Υποχρεωτικό checkbox αποδοχής πολιτικής ακύρωσης πριν την πληρωμή.
- [x] **Email επιβεβαίωσης κράτησης** μέσω Mailjet — from `kratiseis@casaplaya.gr`,
      reply-to το gmail της επιχείρησης, GR/EN. `src/lib/email/`. Φεύγει και από τις
      δύο διαδρομές που κάνουν μια κράτηση πληρωμένη (callback τράπεζας και δωρεάν
      προϊόντα) και **δεν μπορεί να ρίξει** το callback. Χωρίς credentials το μήνυμα
      καταγράφεται στα logs αντί να σταλεί — άρα λειτουργεί ήδη, λείπουν μόνο τα
      κλειδιά από τη ZanteWize.
- [x] Panel προσωπικού στο `/admin` — `src/app/admin/`, auth με JWT σε httpOnly cookie
      (`src/lib/staff/`). Λίστα ημέρας με σύνοψη, αναζήτηση σε όλες τις ημερομηνίες,
      σελίδα κράτησης με στοιχεία πληρωμής και `SupportReferenceID`, αλλαγή
      ημερομηνίας εντός X−2/X+2 με έλεγχο διαθεσιμότητας. Χρήστες: `npm run staff`.
- [x] Footer με επωνυμία/ΑΦΜ/διεύθυνση σε κάθε σελίδα + σελίδες `/terms` και
      `/privacy` σε GR/EN. **Τα κείμενα είναι προσχέδιο** — σημειώνονται ως τέτοιο
      στη σελίδα και θέλουν έγκριση από την επιχείρηση (ενότητα Β).
- [x] Εικονίδια/σήματα καρτών στο δικό μας site — `src/lib/payment-brands.ts` +
      `PaymentTrustStrip` στην αρχική και στο `/security` (§8).
- [x] Οδηγίες παράδοσης — `docs/odigies-paradosis.md` (χρήση panel, αλλαγή
      ημερομηνίας, επιστροφές μέσω AdminTool, τι θέλει τεχνικό).
- [ ] Vercel: `AUTH_SECRET` με πραγματικό μυστικό (`openssl rand -hex 32`) —
      τοπικά είναι ακόμα `dev-only-change-me`. Χωρίς αυτό δεν ανοίγει το `/admin`.
- [ ] Δημιουργία των πραγματικών λογαριασμών προσωπικού (`npm run staff`).
- [ ] Φωτογραφίες προϊόντων — η αρχική δείχνει γκρι πλαίσια.

## Ε. Euronet / epay

- [x] **Υπάρχει ήδη εμπορική σχέση με Πειραιώς** — η επιχείρηση έχει φυσικό POS
      (2026-07-31). Άρα δεν ξεκινάει νέα σύμβαση αποδοχής καρτών από το μηδέν, μόνο
      **επέκταση** του υπάρχοντος λογαριασμού με ηλεκτρονική αποδοχή.
- [x] Η αίτηση του §3 είναι **πλήρης** — στοιχεία τεχνικού υπευθύνου, στοιχεία
      επιχείρησης, τα 5 URL, IP `46.224.113.245`, POST, χωρίς δόσεις.
      Όλα στο `docs/epay-onboarding.md` §1.
- [ ] **Email στον εμπορικό εκπρόσωπο** epay/Πειραιώς → ζητάμε επέκταση + την επαφή του
      τεχνικού τμήματος. Κείμενο έτοιμο: `docs/email-epay.md`. **Αυτό είναι το μόνο που
      μπλοκάρει τα πάντα τώρα.**
- [ ] Αποστολή αίτησης **test account** (§3) — μόλις δοθεί ο παραλήπτης.
- [ ] Παραλαβή των 5 credentials (test).
- [ ] `PAYCENTER_MODE=test` + credentials στο Vercel.
- [ ] Τα 3 **υποχρεωτικά test cases** (§7): APPROVED / DECLINED / RECHARGE ATTEMPT.
- [x] **Apple Pay — απαντήθηκε, δεν ρωτάμε τίποτα** (2026-07-28, από τον οδηγό
      «ApplePay GooglePay v1.0»). Αφορά αποκλειστικά direct integration: η επιχείρηση
      διασυνδέεται η ίδια με Apple και στέλνει token/CAVV/Eci στο Transaction WS. Με
      φιλοξενούμενη σελίδα δεν αγγίζουμε ποτέ αυτά τα δεδομένα, οπότε Apple Pay θα
      σήμαινε εγκατάλειψη του Redirection → PCI scope, δική μας φόρμα κάρτας, δικό
      μας 3DS. **Δεν το προτείνω.** Το Google Pay δουλεύει αυτόματα.
- [ ] Προαιρετικό ερώτημα, **δεν μπλοκάρει**: δέχονται εύρος IP ή δεύτερη IP; (Θα
      χρειαζόταν μόνο αν θέλαμε εφεδρικό proxy.)
- [ ] Αίτηση **live account**: live URLs + live IP → τα 5 live credentials.
- [ ] Άρση κλειδώματος (`SITE_GATE_PASSWORD` κενό), `PAYCENTER_MODE=live`.
- [ ] Εντολή προς ZanteWize για αλλαγή του BOOK NOW.
